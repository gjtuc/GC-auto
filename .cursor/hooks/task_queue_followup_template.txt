# =============================================================================
# task_queue_followup_template.txt
# continue Hook 이 다음 단계 followup_message 를 만들 때 쓰는 **본문 템플릿**
# =============================================================================
#
# 치환 (1곳만):
#   (placeholder TASK on line "현재 단계:" only)
#
# 수정 시:
#   · 에이전트에게 매 턴마다 반복하고 싶은 규칙을 여기에 적으면 됨
#   · INIT_MESSAGE.md 긴 개시지시와 **의도를 맞추는 것**을 권장
#
# =============================================================================

.cursor/AGENT_TASK_QUEUE.md 작업 큐 — 아래 단계 하나만 진행하라.

현재 단계: {{TASK}}

- 이 단계만 코드 작업 (다른 단계 건드리지 말 것)
- 정적 검증 후 반드시 직접 실행·테스트 (코드 검증과 실행 검증은 별개)
- 생성물이 의도에 맞는지 테스트 결과로 확인, 코드 재검토, 주석
- 완료하면 해당 줄을 [x]로 바꾸고 짧게 보고
- "다음 할까요?" 묻지 말 것 (stop Hook이 이어줌)
- 사람만 가능한 블로커(비밀번호·Origin 실행·장비 PC 실측 등)면 agent_queue_state.json → status=blocked, armed=false
